import { SortCommandRequestBody } from './sort-command-request-body';

describe('SortCommandRequestBody', () => {
  it('should create an instance', () => {
    expect(new SortCommandRequestBody()).toBeTruthy();
  });
});
